apiVersion: 1

policies:
  - orgId: 1
    receiver: IMS-Dev-Null
    group_by: ["alertname", "severity"]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 1h
    routes:
      - receiver: IMS-Dev-Null
        matchers:
          - alertname =~ ".*"
        repeat_interval: 1h
