import { WarehouseMapContainer } from "@/components/warehouse/warehouse-map-container";

export const metadata = { title: "Mapa do Armazém — IMS" };

export default function WarehouseMapPage() {
  return <WarehouseMapContainer />;
}
